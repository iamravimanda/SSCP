import { TenantStatusPipePipe } from './tenant-status-pipe.pipe';

describe('TenantStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TenantStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
