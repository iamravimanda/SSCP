import { TenantStatusIconPipePipe } from './tenant-status-icon-pipe.pipe';

describe('TenantStatusIconPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TenantStatusIconPipePipe();
    expect(pipe).toBeTruthy();
  });
});
